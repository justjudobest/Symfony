{% extends 'base.html.twig' %}

{% block title %}AttributeProduct{% endblock %}

{% block body %}
    <h1>AttributeProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attribute_product.id }}</td>
            </tr>
            <tr>
                <th>Product_id</th>
                <td>{{ attribute_product.productId }}</td>
            </tr>
            <tr>
                <th>Attribute_id</th>
                <td>{{ attribute_product.attributeId }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ attribute_product.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('attribute_product_index') }}">back to list</a>

    <a href="{{ path('attribute_product_edit', {'id': attribute_product.id}) }}">edit</a>

    {{ include('attribute_product/_delete_form.html.twig') }}
{% endblock %}
